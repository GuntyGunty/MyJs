import deleteEl from './delete-el.js';

export default (dropList) => {
	//if (inputEl.value === ``) {
	//	const myDropList = document.querySelector('.drop-list__list');
	//	console.log(myDropList);
	//	const dropEl = myDropList.cloneNode(true);
	//	myDropList.parentNode.innerHTML = dropEl;
	//}
}